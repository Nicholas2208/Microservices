server:
  port: 9000
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"
  profiles:
    active:
      - "prod"

cards:
  message: "Welcome to White Bank cards related local APIs "
  contactDetails:
    name: "Nicholas White - Developer"
    email: "nwhite@mail.com"
  onCallSupport:
    - (812) 333-2654
    - (812) 555-3675
